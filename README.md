# ADMM-for-Sparse-Deep-Neural-Network-Pruning-USING-CNN-INN

This repository includes only the pure ADMM implementation for the CNN model, with the intent of preventing reproduction of results via trivial architectural changes and misleading performance improvements.
